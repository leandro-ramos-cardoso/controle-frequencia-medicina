# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=   # apenas em funções server-side, nunca no frontend

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_TIMEZONE=America/Fortaleza

# E-mail (notificações)
EMAIL_FROM=
EMAIL_SMTP_HOST=
EMAIL_SMTP_PORT=
EMAIL_SMTP_USER=
EMAIL_SMTP_PASSWORD=

# Geofence (valores padrão, sobrescrevíveis em system_settings)
DEFAULT_GEOFENCE_ALLOWED_METERS=100
DEFAULT_GEOFENCE_WARNING_METERS=150
